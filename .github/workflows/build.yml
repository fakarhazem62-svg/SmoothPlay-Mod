name: Build SmoothPlay Mod

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.10'

      - name: Build with Gradle
        run: gradle build --no-daemon

      - name: List build output
        run: |
          echo "=== build/libs ==="
          ls -lah build/libs/ 2>/dev/null || echo "NO build/libs directory"
          find . -name "*.jar" 2>/dev/null

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: SmoothPlay-Mod
          path: |
            build/libs/*.jar
            !build/libs/*-dev.jar
            !build/libs/*-sources.jar
          if-no-files-found: error
